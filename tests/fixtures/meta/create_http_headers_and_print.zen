Given nothing

When I create the 'string dictionary' named 'http headers'
When I create the 'string dictionary' named 'response'
When set 'Cache-Control' to 'no-store' as 'string'
and I move 'Cache-Control' in 'response'
and I move 'response' in 'http headers'

Then print the 'http_headers'